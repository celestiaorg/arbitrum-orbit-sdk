export type CelestiaConfig = {
    enable: true;
    rpc: string;
    tendermint_rpc: string;
    namespace_id: string;
    auth_token: string;
    is_poster: boolean;
    gas_price: number;
    event_hannel_size: number;
    blobstreamx_address: string;
}